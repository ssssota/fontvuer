{
  "scripts": {
    "fmt": "run-p fmt:*",
    "fmt:prettier": "prettier --write .",
    "fmt:rustfmt": "cargo fmt",
    "build": "npm run tauri build --prefix app",
    "dev": "npm run tauri dev --prefix app"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1"
  },
  "packageManager": "pnpm@7.7.0"
}
